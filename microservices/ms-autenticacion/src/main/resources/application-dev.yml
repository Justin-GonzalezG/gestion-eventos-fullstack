eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

logging:
  level:
    root: INFO
    cl.eventos.ms_autenticacion: DEBUG
  file:
    name: logs/ms-autenticacion.log
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"
