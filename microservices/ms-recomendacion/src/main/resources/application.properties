# Puerto y Nombre del servicio
server.port=8088
spring.application.name=ms-recomendacion

# Evita la caida por el config del server, ademas que debe ser obligatorio
spring.cloud.config.enabled=false
spring.cloud.config.import-check.enabled=false

# Conexión a Base de Datos MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/recomendaciones_db?createDatabaseIfNotExist=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Conexiones con los ms de Usuarios y Eventos
ms.eventos.url=http://localhost:8083/api/eventos
ms.usuarios.url=http://localhost:8082/api/usuarios

# Configuración de Hibernate / JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Conexion con el servidor de Eureka
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
